# shared-observability

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-observability` to build the library.

## Running unit tests

Run `nx test shared-observability` to execute the unit tests via [Vitest](https://vitest.dev/).
