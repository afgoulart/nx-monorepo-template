# workflow-b

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build workflow-b` to build the library.

## Running unit tests

Run `nx test workflow-b` to execute the unit tests via [Vitest](https://vitest.dev/).
