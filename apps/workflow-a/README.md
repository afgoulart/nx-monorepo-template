# workflow-a

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build workflow-a` to build the library.

## Running unit tests

Run `nx test workflow-a` to execute the unit tests via [Vitest](https://vitest.dev/).
