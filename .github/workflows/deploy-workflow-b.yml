name: deploy-workflow-b

on:
  push:
    branches:
      - main
    paths:
      - apps/workflow-b/**
      - infra/workflow-b/**

jobs:
  deploy:
    uses: ./.github/workflows/deploy-workflow.yml
    with:
      workflow_name: workflow-b
    secrets: inherit
